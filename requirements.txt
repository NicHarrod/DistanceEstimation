# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=8.0.0
tqdm>=4.60.0
certifi

# PyTorch (if not using conda, uncomment the appropriate line)
# torch>=2.0.0
# torchvision>=0.15.0

# Model libraries
ultralytics>=8.0.0
segment-anything>=1.0
timm>=0.9.0
transformers>=4.30.0
einops>=0.7.0

# GUI (optional, only needed for GUI mode)
toga>=0.4.0
toga-gtk>=0.4.0; sys_platform == 'linux'
toga-cocoa>=0.4.0; sys_platform == 'darwin'
toga-winforms>=0.4.0; sys_platform == 'win32'

# Optional for desktop notifications in GUI
desktop-notifier>=3.5.0